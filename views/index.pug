extends ./layout

block body
    h1 Hello world, you filthy animals!
    p This is coming from the <code>views/index.pug</code> file.

    form(method="POST")
        input(type='text' name='name')
        button(type='submit') Submit
